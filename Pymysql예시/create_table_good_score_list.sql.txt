create table good_score_list (
	id int auto_increment primary key,
	sname varchar(20),
    cname varchar(20),
    midterm int,
    final int,
    medium float,
    enter_date datetime default now()
);